{
  "model_name": "BridgeAdaptCLIP-v1.6",
  "parent_model": "BridgeAdaptCLIP-v1.5",
  "single_controlled_change": "replace uniform hard-positive sampling with skeleton-balanced 128 global + 128 skeleton sampling",
  "ranking_name": "skeleton-balanced hard-pixel pairwise ranking",
  "ranking_loss_weight": 0.01,
  "global_positive_count_per_image": 128,
  "skeleton_positive_count_per_image": 128,
  "hard_negative_count_per_image": 256,
  "skeletonization": "skimage.morphology.skeletonize on native binary GT",
  "skeleton_shortfall_policy": "use all skeleton pixels then supplement from non-skeleton GT positives",
  "gate_loss_weight": 0.1,
  "preserve_loss_weight": 0.01,
  "signed_correction_loss_weight": 0.05,
  "margin_loss_weight": 0.0,
  "row0_checkpoint": "Protocol-v2 Epoch 14",
  "semantic_base_frozen": true,
  "fresh_structural_initialization": true,
  "optimizer": "Adam",
  "optimizer_betas": [0.5, 0.999],
  "learning_rate": 0.0003,
  "physical_batch_size": 4,
  "gradient_accumulation_steps": 2,
  "effective_batch_size": 8,
  "epochs": 15,
  "train_seed": 10,
  "checkpoint_selection": "validation overall native-1024 P-AP",
  "test_policy": "one test after checkpoint selection"
}
