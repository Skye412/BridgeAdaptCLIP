{
  "model_name": "BridgeAdaptCLIP-v1.2",
  "parent_model": "BridgeAdaptCLIP-v1.1",
  "controlled_change": "continuous Row-0-error gate supervision and per-image semantic preservation",
  "initialization": "fresh structural branch from the same Row-0 identity start as v1.1",
  "semantic_base": "frozen Protocol-v2 Row0 Epoch 14",
  "gate_target": "stopgrad(abs(Y_native1024 - P_row0_native1024))",
  "gate_supervision": "BCEWithLogits(A_1024, E_1024)",
  "gate_loss_weight": 0.1,
  "preserve_loss": "per-image weighted mean of (1-E)*abs(G*R)",
  "preserve_loss_weight": 0.01,
  "residual": "bidirectional_unbounded",
  "clip_input_size": 518,
  "structural_input_size": 1024,
  "metric_resolution": 1024,
  "optimizer": "Adam",
  "optimizer_betas": [0.5, 0.999],
  "new_module_learning_rate": 0.0003,
  "weight_decay": 0.0,
  "physical_batch_size": 4,
  "gradient_accumulation_steps": 2,
  "effective_batch_size": 8,
  "max_epochs": 15,
  "train_seed": 10,
  "checkpoint_selection": "validation overall native1024 P-AP only",
  "test_policy": "once_after_validation_selection",
  "diagnostics_do_not_select_checkpoint": true
}
