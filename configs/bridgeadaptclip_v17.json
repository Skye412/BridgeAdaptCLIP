{
  "model_name": "BridgeAdaptCLIP-v1.7",
  "parent_model": "BridgeAdaptCLIP-v1.5",
  "single_controlled_change": "route 80% of pairwise-ranking gradient to hard positives and 20% to hard negatives",
  "fusion": "Z_final = Z_row0 + G * R",
  "gate_loss_weight": 0.1,
  "preserve_loss_weight": 0.01,
  "signed_correction_loss_weight": 0.05,
  "margin_loss_weight": 0.0,
  "ranking_loss": "0.8*softplus(stopgrad(Zn)-Zp) + 0.2*softplus(Zn-stopgrad(Zp))",
  "ranking_loss_weight": 0.01,
  "raise_positive_weight": 0.8,
  "suppress_negative_weight": 0.2,
  "hard_positive_count_per_image": 256,
  "hard_negative_count_per_image": 256,
  "skeleton_balanced_sampling": false,
  "normal_images_in_ranking": false,
  "row0_checkpoint": "Protocol-v2 Epoch 14",
  "semantic_base_frozen": true,
  "fresh_structural_initialization": true,
  "optimizer": "Adam",
  "optimizer_betas": [0.5, 0.999],
  "learning_rate": 0.0003,
  "physical_batch_size": 4,
  "gradient_accumulation_steps": 2,
  "effective_batch_size": 8,
  "epochs": 15,
  "train_seed": 10,
  "checkpoint_selection": "validation overall native-1024 P-AP",
  "validation_diagnostics": "overall and four per-class P-AP; class metrics do not select checkpoint",
  "test_policy": "one test after checkpoint selection"
}
