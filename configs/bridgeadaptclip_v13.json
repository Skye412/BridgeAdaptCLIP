{
  "model_name": "BridgeAdaptCLIP-v1.3",
  "parent_model": "BridgeAdaptCLIP-v1.2",
  "controlled_change": "add signed error-aware correction loss only",
  "architecture_change": "none",
  "semantic_base": "frozen Protocol-v2 Row0 Epoch 14",
  "gate_loss_weight": 0.1,
  "preserve_loss_weight": 0.01,
  "signed_correction_loss": "balanced per-image YE*softplus(-C) and (1-Y)E*softplus(C)",
  "signed_correction_loss_weight": 0.05,
  "residual": "bidirectional_unbounded",
  "clip_input_size": 518,
  "structural_input_size": 1024,
  "metric_resolution": 1024,
  "optimizer": "Adam",
  "optimizer_betas": [0.5, 0.999],
  "new_module_learning_rate": 0.0003,
  "weight_decay": 0.0,
  "physical_batch_size": 4,
  "gradient_accumulation_steps": 2,
  "effective_batch_size": 8,
  "max_epochs": 15,
  "train_seed": 10,
  "checkpoint_selection": "validation overall native1024 P-AP only",
  "test_policy": "once_after_validation_selection",
  "primary_mechanism_check": "FN-like mean signed correction changes from negative to positive"
}
